# Reference this view as dbt_prod__v_stg_linkedin_campaigns
catalog: DBT
schema: PROD
table_name: V_STG_LINKEDIN_CAMPAIGNS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

  campaign_group_id:
    sql: '"CAMPAIGN_GROUP_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: v_stg_linkedin_campaigns
  target_schema: PROD
  config:
    materialized: view
  code: |-
    select id
             , name
             , campaign_group_id
        from FIVETRAN_DATABASE.LINKEDIN_ADS.CAMPAIGN_HISTORY
        qualify row_number() over(partition by id order by last_modified_time desc) = 1
  referenced_by: [ int_linkedin_spend, int_linkedin_conversions ]
