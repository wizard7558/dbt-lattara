# Reference this view as omni_dbt__v_stg_linkedin_conversion_history
schema_label: ""

schema: omni_dbt
table_name: V_STG_LINKEDIN_CONVERSION_HISTORY

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: v_stg_linkedin_conversion_history
  target_schema: PROD
  config:
    materialized: view
  code: |-
    select id
             , name
        from FIVETRAN_DATABASE.LINKEDIN_ADS.CONVERSION_HISTORY
        qualify row_number() over(partition by id order by last_modified desc) = 1
  referenced_by: [ int_linkedin_conversions ]
